{
  "name": "elearny-lms-monorepo",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev:web": "npm --prefix frontend/web run dev",
    "dev:mobile": "npm --prefix frontend/mobile run start",
    "build:web": "npm --prefix frontend/web run build"
  },
  "workspaces": [
    "frontend/web",
    "frontend/mobile"
  ],
  "devDependencies": {
    "expo": "~57.0.22"
  }
}
