{
  "name": "elearny-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.5"
  }
}
